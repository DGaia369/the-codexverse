import { PendingPageShell } from '@/components/public/PublicProse';

// Accessibility / Support — structural shell only. Final accessibility
// claims and support-contact language will be supplied separately and
// professionally reviewed before launch at scale. Do not add
// accessibility claims or support language to this file until then.
export default function AccessibilitySupportPage() {
  return (
    <main>
      <PendingPageShell title="Accessibility / Support" />
    </main>
  );
}
