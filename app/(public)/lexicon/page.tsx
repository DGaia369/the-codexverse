import { PendingPageShell } from '@/components/public/PublicProse';

// Lexicon — structural shell only. Founder-approved public Lexicon
// content will be supplied separately; do not add definitions or
// terminology to this file until then.
export default function LexiconPage() {
  return (
    <main>
      <PendingPageShell title="Lexicon" />
    </main>
  );
}
