import { PendingPageShell } from '@/components/public/PublicProse';

// Our Promise — structural shell only. Founder-approved participant-facing
// copy will be supplied separately; do not add copy to this file until
// then.
export default function OurPromisePage() {
  return (
    <main>
      <PendingPageShell title="Our Promise" />
    </main>
  );
}
