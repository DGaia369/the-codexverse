# Paste near the top of CLAUDE.md

## Constitutional Repository Rules

STOP before making architectural decisions, participant-facing changes, or code changes.

Read in this order:

1. `docs/GOVERNANCE.md`
2. every file in `docs/canon/`
3. every file in `docs/standards/`
4. relevant files in `docs/architecture/`
5. relevant files in `docs/history/`

The hierarchy is:

Governance → Canon → Standards → Architecture → History → Code

If implementation conflicts with canon or standards, do not silently fix, reinterpret, or choose between them. Surface the discrepancy to Diana Francis and wait for a ruling.

Only Diana Francis may lock, amend, retire, or supersede canon.

At the end of every material work session, complete `docs/SESSION-CLOSE.md` and update the relevant documentation before treating the work as complete.
